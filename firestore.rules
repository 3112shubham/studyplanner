rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all read, write, delete for testing
    match /{document=**} {
      allow read, write, delete: if true;
    }
  }
}
